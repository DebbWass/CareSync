/**
 * Patient settings tab — language, contrast, text size.
 */
import { SettingsPanel } from '../../src/components/settings/SettingsPanel';

export default function PatientSettings() {
  return <SettingsPanel />;
}
